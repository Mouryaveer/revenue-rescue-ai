# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Razorpay (test mode only — never production)
razorpay==1.4.2

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
psycopg2-binary==2.9.10

# Redis / Celery
redis==5.2.0
celery==5.4.0

# AI / Agent — compatible versions for Python 3.12 + Pydantic v2
langchain==0.3.7
langgraph==0.2.45
langchain-openai==0.2.9
openai==1.54.4

# langsmith — let pip resolve compatible version with langchain 0.3.7
langsmith>=0.1.147,<0.2.0

# Data / Simulation — numpy <2.0 required by langchain 0.3.x
pydantic==2.10.0
pydantic-settings==2.6.0
faker==33.0.0
pandas==2.2.3
numpy==1.26.4

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# HTTP client
httpx==0.27.2

# Config
python-dotenv==1.0.1

# Logging
structlog==24.4.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0

# Code quality
ruff==0.7.1
mypy==1.13.0
bandit==1.8.0
